# SeeTalk（AI视觉对话助手）

Version: 1.0

Runtime: Python 3.11

Framework: Flask + gevent

Database: SQLite

AI Platform: MiniMax-M3（Anthropic 兼容直连，原生多模态）+ 阿里百炼 Paraformer ASR + 浏览器 Web Speech

Client: 响应式 Web（桌面 + 移动浏览器）；Android WebView 套壳为 stretch

---

# 目录

1. 项目简介
2. 用户洞察
3. 产品定位
4. 产品目标
5. 用户故事
6. 功能实现情况
7. 核心功能
8. 技术架构
9. AI架构
10. 多模态理解设计
11. 对话引擎设计
12. 视觉理解设计
13. 语音交互设计
14. 成本控制设计
15. Dashboard
16. QoS设计
17. Evaluation Framework
18. A/B Test设计
19. 埋点设计
20. 数据模型
21. 项目结构
22. 测试设计
23. Demo设计
24. 商业化思考
25. 安全设计
26. Git协作原则
27. AI辅助开发声明
28. Definition Of Done

---

# 1. 项目简介

SeeTalk 是一款实时多模态 AI 对话助手。

用户打开：

* 摄像头
* 麦克风

AI能够：

* 看见环境
* 听见用户
* 理解上下文
* 自然交流

帮助用户完成：

* 学习
* 办公
* 生活辅助
* 内容理解

---

# 2. 用户洞察

用户真正需要的不是：

"拍照问AI"

而是：

"像和真人交流一样"

例如：

用户拿起一本书：

"这本书讲什么？"

AI 无需用户手动拍照上传。

系统在用户提问的瞬间自动抓取当前一帧。

体验上"直接问、直接答"（无需上传 = 无需手动操作，非不传数据）。

---

用户指向电脑报错：

"为什么报错？"

AI直接分析。

---

用户指向冰箱：

"这些食材能做什么？"

AI直接建议。

---

# 3. 产品定位

不是：

ChatGPT网页版

---

不是：

OCR工具

---

定位：

实时视觉对话助手

---

核心价值：

让AI持续感知用户环境。

---

# 4. 产品目标

实现：

视觉输入

*

语音输入

↓

实时理解

↓

自然回应

---

整体交互接近人与人沟通。

---

# 5. 用户故事

## 设计原则：1 条管线 × N 个场景

所有故事共用同一条「多模态看图 + 提问 + 回答」管线，区别只在场景预设 / 提示词，不在工程。

因此：列得多（产品想象力），实现少而精（hero 场景深做），诚实标注其余为规划。

---

## 状态口径（单一真相源，§6 与此对齐）

* ✅ 已实现：MVP，已落地并测试
* 🟡 部分：可跑通但未完善
* ⬜ 规划：想到、未实现

"计划"= 设计时打算做；"最终实现"= 提交时真实状态，诚实回填。

---

## MVP（hero，深做 + Demo）

| Story  | 场景 | 计划  | 最终实现 |
| ------ | -- | --- | ---- |
| 举教材讲解  | 学习 | MVP | ✅    |
| 屏幕报错分析 | 开发 | MVP | ✅    |
| 英文实时翻译 | 学习 | MVP | ✅    |
| 食材推荐菜谱 | 生活 | MVP | ✅    |

> M3 原生多模态可直接看像素，4 个 hero 全部可行。文档阅读 / PPT 讲解与"教材讲解"同一条管线，作为"已支持场景"。

---

## Stretch / 规划（想到的故事，列入产品蓝图）

| Story            | 场景 | 优先级     | 最终实现 |
| ---------------- | -- | ------- | ---- |
| 会议记录辅助           | 办公 | Stretch | 🟡 部分 |
| 商品识别             | 生活 | Stretch | ⬜    |
| 景点导览             | 旅行 | Stretch | ⬜    |
| 产品经理白板助手（流程图 Review） | 职场 | 规划      | ⬜    |
| 架构图 / 时序图 Review | 开发 | 规划      | ⬜    |
| 实验室助手（电路板 / 装置原理） | 教育 | 规划      | ⬜    |
| 数学解题（拍题作答）       | 学习 | 规划      | ⬜    |
| 视障环境描述           | 公益 | Vision  | ⬜    |

> 数学题可视化渲染不在 v1，仅让 M3 读题作答。视障描述需无障碍 UX（纯语音、不依赖看屏），列为愿景。

---

# 6. 功能实现情况

| 功能                    | 计划      | 实现  |
| --------------------- | ------- | --- |
| 摄像头接入                 | √       | √   |
| 麦克风接入                 | √       | √   |
| 语音识别（桌面 Web Speech / 移动百炼） | √       | √   |
| 多模态图像理解（M3）           | √       | √   |
| 多轮对话（默认 10 轮文本 + 当前帧） | √       | √   |
| TTS 播报（浏览器 / 百炼）      | √       | √   |
| OCR 优先路由              | √       | √   |
| 按需触发 + 变化检测 + 缓存      | √       | √   |
| 成本实测对账 Dashboard      | √       | √   |
| 连续分析模式（会议辅助）          | √       | 部分  |
| 视觉记忆（多帧历史，付费档）        | √       | 部分  |
| Android WebView 套壳    | Stretch | 未实现 |

---

# 7. 核心功能

## 摄像头实时感知

按需抓帧：仅用户提问（VAD 触发）时抓当前帧。

连续分析为可选模式（会议 / 无障碍），默认关。

---

## 麦克风实时感知

浏览器 VAD 门控：有人说话才开 ASR 流，静默不传（省 ASR 成本）。

---

## 多模态问答

结合：

图像

*

语音

*

历史上下文

回答。

---

## OCR识别

支持：

英文

中文

数字

代码

---

## TTS播报

浏览器 speechSynthesis（免费默认）/ 百炼实时 TTS（付费高音质）。

首句优先：第一句话流式就绪即开口。

---

# 8. 技术架构

```text
浏览器（端·粗闸门）
  ├─ getUserMedia 摄像头 → 客户端限帧 1fps
  ├─ getUserMedia 麦克风 → VAD（有人说话才开流）
  └─ 帧变化检测（可选）
        │  仅"通过闸门"的数据上行
        ▼
Flask 服务（端·自有廉价算力）
  ├─ ASR：桌面 Web Speech(免费) / 移动·百炼 Paraformer(wss 代理)
  ├─ 抓当前帧 → 降分 512px
  ├─ OCR 优先路由（本地 Tesseract）
  │     ├─ 够文本 → 只发 OCR 文本
  │     └─ 否则   → 发降分整图
  ├─ 感知哈希：变化检测 + 视觉缓存（命中不调云）
  ├─ Context：文本历史(默认10轮) + 当前帧
  └─ Metrics / Token 计费 / Dashboard
        │  唯一付费调用
        ▼
云（付费）
  └─ MiniMax-M3（Anthropic /v1/messages，多模态：图+文+历史）
        │  流式输出
        ▼
回答 → 首句优先 → TTS（浏览器 / 百炼）+ 字幕
```

---

平台：

v1 = 响应式 Web（桌面 + 移动浏览器满血）。

Android = WebView 套壳（stretch，复用「共享后端 + 薄壳」模式）。WebView 内无 Web Speech，故移动语音走百炼。

不做 Flutter（双客户端重写，投入产出比差）。

---

# 9. AI架构

视觉 + 对话：MiniMax-M3 直连 Anthropic 兼容接口 `/v1/messages`，图片以 base64 block 传入（原生多模态，真看像素）。

ASR：阿里百炼 Paraformer 实时（wss，Flask 代理 Key）；桌面可用浏览器 Web Speech 免费兜底。

TTS：浏览器 speechSynthesis（免费默认）/ 百炼实时 TTS（付费高音质）。

OCR：本地 Tesseract（免费），作省钱路由，非视觉主力。

Hermes 网关：仅文本聊天，无图片/ASR/TTS 端点；本项目作可选文本兜底，不承担视觉。

---

## AIService 封装

业务代码不直连模型，统一经 AIService：vision_chat / asr / tts / ocr。

复用 speak_mate 的 mock 层，便于单测。

---

# 10. 多模态理解设计

输入：

图像

语音

历史上下文

---

构建：

Unified Context

---

例如：

图像：

显示一本《计算机网络》

语音：

"这本书适合初学者吗？"

---

AI可正确理解：

"这本书"

指代当前画面。

---

# 11. 对话引擎设计

维护：

Session

Context

Visual State

Conversation History

---

上下文窗口：

* 文本历史：默认 10 轮（可配置 1/3/5/10…，付费档更长）
* 图片：仅当前 1 张（可配置 5/10 张 = 视觉记忆，付费档）

历史只留文本摘要（含"当时看到了什么"一句），不回传旧图。

指代消解（"这本书""刚才那个报错"）靠文本摘要 + 当前帧完成。

每多留一张历史图 = 多一份图片 token，故默认 1 张最省。

Demo 演示跑 ≥5 轮文本以展示多轮自然度，1 轮作免费版地板。

---

# 12. 视觉理解设计

触发：按需（VAD 提问时抓当前帧），非定时采样。

连续分析为可选模式（会议 / 无障碍），开启时降采样更狠（3~5 秒一帧），默认关。

---

避免：

全视频上传。

---

支持：

物体识别

场景识别

OCR

UI理解

截图分析

---

# 13. 语音交互设计

ASR

↓

NLU

↓

Response

↓

TTS

---

默认：免手 VAD 自动断句（浏览器端测能量阈值判断"说完了"）。

---

ASR 分层（= 免费 / 付费产品线）：

| 端    | 默认               | 可切换    |
| ---- | ---------------- | ------ |
| 桌面 Web | 浏览器 Web Speech（免费） | 手动切百炼  |
| 移动端  | 百炼 Paraformer    | —      |

移动端不用浏览器 Web Speech（iOS / WebView 不可靠），统一走百炼。

---

打断式交互（barge-in）：规划中（需 AEC + 流式 + 打断，成本高，v1 不做）。

---

# 14. 成本控制设计

这是本项目重点。

付费项：

* 主要：MiniMax-M3 多模态 token（图片 token 远贵于文本）
* 次要：移动端百炼 ASR（按音频时长，便宜，且被 VAD 门控）

桌面 ASR（Web Speech）/ TTS（浏览器）/ OCR / 变化检测：端侧免费。

成本控制主战场 = M3 图片 token；ASR 由 VAD 门控控量。

---

## Baseline（朴素实现，用于对账）

会话期间每秒 1 帧整图持续发给 M3。

5 分钟会话 ≈ 300 次调用 × ~1300 图片 token ≈ ~39 万 input token（仅视觉）。

所有节省率以此为分母，由 Dashboard 实测得出。

---

## 省钱杠杆（按真实影响排序）

| 杠杆           | 真实影响        | 机制                          | 状态        |
| ------------ | ----------- | --------------------------- | --------- |
| 按需触发         | ~95%↓（决定性）  | 仅用户提问（VAD 触发）时抓帧，静默不调用      | 采用        |
| 图像降分辨率       | 单次 token 再砍 | 发图前缩到 512px 并压缩             | 采用        |
| OCR 优先       | 纯文本场景省掉整张图  | 见下方路由                        | 采用        |
| 视觉缓存 + 变化检测  | 修剪重复场景      | 同画面感知哈希命中则复用，不调云            | 采用        |
| 短上下文         | 控制文本侧增长     | 仅保留最近 N 轮                    | 采用        |
| 全量视频上传       | ——          | ——                          | 未采用（成本极高） |

主力是按需触发（约两个数量级下降），其余仅修剪剩余零头。

---

## OCR 优先路由

按需抓帧后，先跑本地免费 Tesseract OCR。

提取出 ≥ N 个高置信字符：

→ 判为文本场景 → 只发 OCR 文本（可选附缩略图），省掉整图 token。

否则：

→ 发降分辨率整图给 M3。

误判风险（如"这本书什么颜色"被路由成纯文本）接受，由 §18 A/B（OCR优先 启用 vs 关闭）量化。

---

## 节省率：以实测为准

不写固定百分比（避免伪指标，呼应 §17）。

Dashboard 同时记录：

* 实际消耗 token
* 按 Baseline 假设的 token

当场算出节省率，真实对账。

---

# 15. Dashboard

地址：

/dashboard

---

展示：

会话数

识别次数

OCR次数

TTS次数

平均响应时间

Token消耗

成本估算

---

# 16. QoS设计

指标以实测为准，不写死承诺。

---

## 首句优先（感知延迟的关键）

M3 流式输出，第一句话一出就喂 TTS 开口。

首响应 = time-to-first-sentence，而非整段完成。

复用 speak_mate 首句优先策略。

---

## 按回合类型分桶（混在一起即伪指标）

| 回合类型     | 链路            | 现实首音目标（软目标） |
| -------- | ------------- | ----------- |
| 纯文本/OCR  | 只发文本，token 少  | < ~1.2s     |
| 带图多模态    | 发降分图，TTFT 高   | < ~2.5s     |

不对带图回合承诺 <1s（带图 TTFT 通常 1~3s 起，臆断必被戳穿）。

---

## 实测分布

Dashboard 按回合类型分桶记录真实延迟。

报告 P50 / P95 实测分布，不臆断固定阈值。

---

## 成功率

ASR 成功率、视觉回答成功率同样实测统计，不预先承诺数字。

---

# 17. Evaluation Framework

避免伪指标。

---

## Scene Understanding

视觉理解正确率

---

## OCR Accuracy

OCR准确率

---

## Conversation Success

用户问题被正确回答比例

---

## Latency

端到端延迟

---

## User Satisfaction

用户反馈评分

---

# 18. A/B Test设计

## 触发策略

按需触发

VS

连续分析

---

比较：

成本

准确率

视觉信息完整度

---

## OCR优先策略

启用

VS

关闭

---

比较：

Token消耗

响应速度

---

# 19. 埋点设计

track(event)

---

camera_open

camera_close

---

voice_start

voice_stop

---

ocr_success

ocr_fail

---

vision_success

vision_fail

---

cache_hit

ocr_route（text / image）

asr_provider（webspeech / bailian）

---

tts_start

tts_finish

---

dashboard_open

---

# 20. 数据模型

Session

Conversation（文本历史，含视觉摘要）

FrameAnalysis（感知哈希 + 结果，不存原图）

OCRRecord

Metrics（实际 token / Baseline token / 延迟分桶）

EventLog

PlanConfig（档位：ASR 提供方 / 上下文轮数 / 视觉记忆帧数 → 映射付费等级）

---

## 并发与存储

Flask + gevent 协程并发；SQLite 开 WAL 模式。

metrics / 埋点高频写：批量 + 异步落库，避免每帧同步写阻塞。

单用户 Demo 量级压力小；多用户时存储层可平滑换 Postgres。

---

# 21. 项目结构

seetalk/

app.py

config.py

db.py

ai_service.py

camera_service.py

asr_service.py

vision_service.py

ocr_service.py

conversation.py

tts_service.py

metrics.py

dashboard.py

templates/

static/

tests/

README.md

design.md

---

# 22. 测试设计

pytest

pytest-cov

---

策略：

Mock 两个云（MiniMax / 百炼），复用 speak_mate/ai/mock.py 套路。

确定性逻辑测到高覆盖：VAD 门控、变化检测、视觉缓存、OCR 路由、上下文截断、token 计费、成本对账、Dashboard、数据模型。

不测 AI 答案本身（非确定性），只测"给定 mock 响应，管线行为正确"。

---

目标：

覆盖率尽量高，实测值写入 README（不硬承诺固定百分比）。

---

# 23. Demo设计

演示：

教材问答

↓

屏幕报错分析

↓

英文翻译

↓

食材识别

↓

Dashboard

---

时长：

5~8分钟

---

# 24. 商业化思考

个人版：

学习助手

生活助手

---

专业版：

开发辅助

会议助手

---

企业版：

客服

培训

现场运维

---

## 免费 / 付费 = 配置档位

| 能力   | 免费版            | 付费版                 |
| ---- | -------------- | ------------------- |
| 语音   | 浏览器 Web Speech | 百炼（更准 + 移动满血 + 高音质 TTS） |
| 上下文  | 文本 1~3 轮       | 10~20 轮             |
| 视觉记忆 | 当前 1 帧         | 5~10 帧              |
| 连续分析 | 关              | 开（会议辅助）             |

PlanConfig 层统一承载，技术验证后按档位放量。

---

# 25. 安全设计

## 密钥安全

禁止提交：API_KEY / AccessToken / Cookie / 云认证信息。

统一走 .env / config.json，Key 仅存服务端，前端不可见（百炼 wss 由 Flask 代理）。

---

## 用户数据隐私（持续感知，数据流向 MiniMax / 百炼）

| 措施     | 做法                              | DEMO 阶段     |
| ------ | ------------------------------- | ----------- |
| 数据最小化  | 按需触发 + VAD 门控 + 变化检测（省钱机制即隐私机制） | ✅ 已具备       |
| 明示同意   | 首次显式授权 + UI 常驻"👁正在看 / 🎤正在听"   | ✅ 落地        |
| 不存原始媒体 | 默认不落盘帧 / 音频，只存文本与哈希            | 设计已述，Demo 从简 |
| 第三方告知  | 明确告知数据发往 MiniMax / 阿里百炼         | ✅ 告知        |
| 用户可控   | 一键暂停采集 / 清除会话                   | 设计已述，Demo 从简 |

现阶段以用户体验为主：DEMO 重点落地"明示告知"，其余作设计声明。

核心论点：传得越少，既省钱又更私密——成本设计与隐私设计同源。

---

# 26. Git协作原则

采用自然迭代。

每个PR：

对应一个完整功能。

例如：

PR1

摄像头接入

---

PR2

ASR接入

---

PR3

视觉理解

---

PR4

OCR

---

PR5

TTS

---

PR6

Dashboard

---

# 27. AI辅助开发声明

本项目在 AI 辅助下开发，如实声明（与 README「AI 协作说明」一致）。

---

## 允许的协作工具

Hermes

Claude Code

Codex

Cursor Agent

OpenHands

---

## AI 承担

与作者对话打磨本设计文档（端云分工 / 成本模型 / QoS / 隐私 等逐条达成共识）。

按 §26 的 PR 顺序实现各模块，并配套 Mock 云的确定性单测。

接入真实云能力时逐个真机验证：

* M3 多模态 / 流式
* 百炼 ASR 语音往返
* CosyVoice TTS
* 缓存命中
* OCR 路由
* A/B
* 评估
* 会议纪要

---

## 人类作者承担

提供技术栈约束与真实 API Key。

关键产品取舍（平台 / 语音模态 / 付费分层 等）由作者拍板。

审阅 PR 与合并，把控方向。

---

## 底线

实现与文档一致：

本文档描述的能力均有对应实现与测试；未实现项在 §5 / §6 如实标注。

不冒充：

无 Key 时降级产出明确标注「示例 / 降级」；指标以实测为准（Dashboard / 评估框架），不写死伪指标。

密钥安全：

全程不将任何 Key 写入仓库，提交前自动扫描兜底（见 §25）。

---

# 28. Definition Of Done

满足：

* 摄像头接入完成
* 麦克风接入完成
* ASR完成
* Vision完成
* OCR完成
* 多轮对话完成
* TTS完成
* Dashboard完成
* QoS统计完成
* 成本统计完成
* MVP（4 个 hero 故事）完成，其余诚实标注状态
* 单元测试覆盖率：实测并在 README 披露（尽量高）
* README完整
* Demo完整
* design.md完整

项目方可标记完成。

