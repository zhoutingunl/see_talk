# SeeTalk 提交说明（评审导览）

> 面向评审/提交方的总览。本文聚焦题目要求的**两份交付物**(用户故事、成本控制),
> 其余不在此重复:完整设计见 [`design.md`](./design.md),运行/测试见 [`README.md`](./README.md),
> 演进过程见仓库 PR 历史 <https://github.com/zhoutingunl/see_talk> (#1–#20)。

SeeTalk 是一款实时多模态 AI 视觉对话助手:打开摄像头与麦克风,AI 能看见环境、听见用户、自然回应。

---

## 0. 如何验证(30 秒)

```bash
# 本机(桌面浏览器)
python3.11 app.py                     # 打开 http://localhost:8000

# 手机(真机验证最终效果)
SEETALK_HTTPS=1 python3.11 app.py     # Android Studio 打开 android/ 一键 Run,填 https://<电脑IP>:8000

# 测试
python3.11 -m pytest -q               # 110 passed 96%(无 chi_sim 中文包则 108 passed + 2 skipped)
```

**已真机验证(非 Mock,用真实 Key)**:M3 多模态看图/流式回答 · 百炼 Paraformer 语音往返 ·
CosyVoice TTS · 视觉缓存命中 · OCR 优先路由(中/英)· A/B 分桶对比 · 评估框架(OCR/QA 1.0)· 连续分析会议纪要。
成本对账见运行后的 `/dashboard`。

---

## 1. 交付物①:用户故事(计划 vs 最终实现)

**设计原则:1 条管线 × N 个场景。** 所有故事共用同一条「多模态看图 + 提问 + 回答」管线,
区别只在场景/提示词。因此**列得多(产品想象力)、实现少而精(hero 深做)、其余诚实标注**。详见 [`design.md §5`](./design.md)。

| Story | 场景 | 计划 | 最终实现 |
| --- | --- | --- | --- |
| 举教材讲解 | 学习 | MVP | ✅ 已实现 |
| 屏幕报错分析 | 开发 | MVP | ✅ 已实现 |
| 英文实时翻译 | 学习 | MVP | ✅ 已实现 |
| 食材推荐菜谱 | 生活 | MVP | ✅ 已实现 |
| 文档阅读 / PPT 讲解 | 办公 | 已支持场景 | ✅ 同管线,直接可用 |
| 会议记录辅助 | 办公 | Stretch | 🟡 基本实现(连续分析 + 纪要,见 `observations.py`) |
| 视障环境描述 | 公益 | 愿景 | 🟡 技术可跑(M3 描述场景),未做无障碍专属 UX |
| 商品识别 / 景点导览 | 生活/旅行 | Stretch | ⬜ 未专门实现(同管线可跑,未单独测/演示) |
| 白板流程图 / 架构图 Review / 实验室 / 数学解题 | 职场/开发/教育/学习 | 规划 | ⬜ 想到、未实现 |

**口径**:✅ 已落地并(多数)真机验证;🟡 基本可用但未完善;⬜ 想到未实现。
"已实现"指按计划落地且测试覆盖,非空头声明——对应代码与 PR 可逐项核对。

---

## 2. 交付物②:成本控制(想到 vs 实际采用)

**核心认知**:本系统唯一**主要付费项 = MiniMax-M3 多模态调用的 token**(图片 token 远贵于文本);
ASR 为小头(按时长、VAD 门控);桌面 ASR/TTS、OCR、缓存、变化检测全在端侧免费。
所有成本手段都在削这一个数。完整论证见 [`design.md §14`](./design.md)。

| 技巧 | 想到 | 采用 | 真实影响 / 说明 |
| --- | --- | --- | --- |
| 全量视频上传 | ✓ | ❌ 未采用 | 反例,成本极高,明确弃用 |
| **按需触发**(仅用户提问/VAD 时抓帧) | ✓ | ✅ | **决定性(约两个数量级↓)**,主力 |
| 浏览器端限帧(不上传 30fps 视频) | ✓ | ✅ | 端侧粗闸门,数据离开端前就节流 |
| 图像降分辨率(512px)+ 压缩 | ✓ | ✅ | 单次图片 token 再砍一截 |
| **OCR 优先**(纯文本场景只发文本) | ✓ | ✅ | 报错/英文/中文文本省掉整图 token(本地 Tesseract) |
| 视觉缓存 + 变化检测(感知哈希) | ✓ | ✅ | 同画面同问命中即复用、零云调用;连续模式画面没变跳过 |
| 短上下文(默认 10 轮文本 + 仅当前 1 帧) | ✓ | ✅ | 历史只存**文本摘要**,绝不回传旧图 |
| VAD 门控 ASR(没人说话不开流) | ✓ | ✅ | 控 ASR 用量;音频在端侧就被挡 |
| 分层免费/付费(桌面 Web Speech 免费档) | ✓ | ✅ | 免费=浏览器语音,付费=百炼,映射 PlanConfig |
| 端侧物体检测代替部分云调用 | ✓ | ❌ 未采用 | M3 原生多模态够用,未引入端侧 VLM,避免复杂度 |
| **实测对账**(非伪指标) | ✓ | ✅ | Dashboard 记 实际 token vs Baseline → 节省率;实测 **95%+** |

**端云协同**:浏览器(限帧/VAD/变化检测粗闸门)→ Flask(缓存/OCR 路由/上下文细闸门)→ 仅必要时调云。
省钱机制同时是隐私机制(传得越少越省越私密)。节省率**以 Dashboard 实测为准,不写死百分比**(呼应"避免伪指标")。

---

## 3. 对照题目三大维度

| 维度 | 实现要点 | 真机证据 |
| --- | --- | --- |
| 视觉理解准确性 | M3 原生多模态直看像素;OCR 路由提文本;聚焦手持前景物(避人脸干扰) | 评估框架 `evaluation.py`(OCR/QA 准确率)、Dashboard |
| 语音交互自然度 | 免手 VAD + 中文优先百炼 ASR + 流式回答 + 首句优先 TTS + 打断式停播 | 百炼语音往返、CosyVoice 合成、SSE 流式 |
| 端云协同成本控制 | 见交付物②;两级闸门 + 实测对账 | `/dashboard` 节省率 95%+ |

---

## 4. 工程与质量(引用,不重复)

- **演进**:20 个 PR(中文 `[feat]/[fix]` 前缀),逐个真机验证后合入,见仓库 PR 历史。
- **测试**:110 passed,覆盖率 96%(未装 chi_sim 中文包则 108 passed + 2 skipped)。策略=Mock 云 + 确定性逻辑(`README.md` 测试节)。
- **作者署名**:同一作者(git 本地名 `zhouting03` / GitHub 账号 `zhoutingunl`,同一人;邮箱不同不构成多人),AI 协作见 design.md §27,提交均含 Claude co-author。
- **安全/隐私**:密钥仅服务端 `.env`(已 gitignore,全历史扫描无泄漏);按需抓帧 + 默认不落盘原始媒体(`design.md §25`)。
- **手机端**:`android/` WebView 套壳,补齐摄像头/麦克风权限 + 自签 HTTPS(`android/README.md`)。

---

## 5. 已知限制与取舍

- 浏览器语音 UI(VAD/录音/Web Speech)依赖真实浏览器 + 麦克风,无法 headless 单测;后端协议均已真机验证。
- 流式下 MiniMax 未回传 input_tokens(非流式正常);成本对账以非流式/Dashboard 为准。
- WebView 内无浏览器语音,移动端语音统一走百炼(需配 `BAILIAN_API_KEY`)。
- 成本单价为占位估算(明确标注),真实以账单为准;节省率为实测。
- Stretch/规划故事(商品/景点/白板等)是同一管线换提示词,未单独实现与演示。

---

## 6. 建议评审路径

1. 读本文两张表(交付物①②)→ 2. `design.md`(端云分工/成本模型/QoS/隐私)→
3. 跑 `python3.11 app.py` 点一遍 + 看 `/dashboard` 节省率 → 4. `python3.11 evaluation.py` 看准确率 →
5. 翻 PR 历史看演进与真机验证记录 → 6. 手机 `android/` 验证最终效果。
